import { ShownLengthPipe } from './shown-length.pipe';

describe('ShownLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new ShownLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
